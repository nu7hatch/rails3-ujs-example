<%- content_tag_for :li, user do %>
  <%= user.name %> <%= user.surname %> (<%= user.email %>) 
  <%= link_to "Delete", user_path(user), :method => :delete, :remote => true %>
  <%= link_to "More", user_path(user, :more => true), :remote => true %>
<% end %>
